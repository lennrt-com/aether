# Convex — also set WORKER_KEY and UNIPILE_WEBHOOK_SECRET in the Convex dashboard
CONVEX_DEPLOYMENT=
CONVEX_URL=
CONVEX_SITE_URL=
WORKER_KEY=

# LLM (Stagehand + persona generation)
GOOGLE_GENERATIVE_AI_API_KEY=

# Chrome
CHROME_EXECUTABLE_PATH=
PINNED_CHROME_VERSION=

# Coronium proxy (per-profile bindings live in Convex after provision)
PROXY_SERVER=
PROXY_USERNAME=
PROXY_PASSWORD=
# PROXY_BYPASS=

# Worker
WORKER_NAME=local-1
MAX_SESSIONS=2
PROFILES_DIR=./.profiles

# Unipile (phase 8)
UNIPILE_DSN=
UNIPILE_API_KEY=
UNIPILE_WEBHOOK_SECRET=

# Hindsight (deferred — stub until HTTP API is wired)
# HINDSIGHT_API_URL=
# HINDSIGHT_API_KEY=

# Dev overrides (prototype / manual browse tasks)
START_URL=https://example.com
AGENT_INSTRUCTION=
MAX_STEPS=15
HEADLESS=false
